# ============================================================
# ROB DOES IT — ENVIRONMENT
# Copy to .env.local and fill in when the real values exist.
# ============================================================

# Where booking enquiries are delivered. Any endpoint that accepts
# a JSON POST works: Zapier / Make / Formspree / Slack webhook / your API.
# Until this is set, submissions are validated and logged on the server
# (and the server log warns that nothing was delivered).
BOOKING_WEBHOOK_URL=
